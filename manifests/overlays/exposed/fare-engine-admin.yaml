# A network policy that restricts nothing is not a restricting network policy.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: fare-engine-admin
spec:
  ingress:
    - from:
        - ipBlock:
            cidr: 0.0.0.0/1
        - ipBlock:
            cidr: 128.0.0.0/1
